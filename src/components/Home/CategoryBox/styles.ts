import { StyleSheet } from 'react-native';
import { CoolGaryColor } from '@components/lds/foundation-color';

const CategoryBoxStyle = StyleSheet.create({
  layout: {
    height: 80,
    backgroundColor: CoolGaryColor['50'],
    padding: 12,
    borderRadius: 12,
  },
});

export default {
  CategoryBoxStyle,
};
